apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: inferenceforge-gateway
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: inferenceforge-gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
